# 次にやること（引き継ぎ）

**最終ゴール**: 「これ風に作って」と言うだけで、**誰でも／自動で**動画編集ができる状態。

**2026-08-02 更新。** 前提知識なしで着手できるように書いています。

---

## いまどこにいるか

| | 状態 |
|---|---|
| 編集の一次指示書 | ✅ 完備（`auto-youtube-edit-agent/`・678行） |
| 静的検査 | ✅ 29項目・違反0（**ただし横1920x1080のみ**。下の②参照） |
| スタイル定義を実際に描画に使う | ✅ `pipeline/style_apply.py`（`load_style` / `render_plan`） |
| **横1920x1080での成立** | ✅ **字幕roleを持つ13本すべて成立**（07-31時点は7本） |
| **縦1080x1920での成立** | ⚠️ **4/13**。残りは構造的（②） |
| 実素材で1本通す | ✅ 2分14秒の縦素材で完走。字幕57件・4辺NG 0件 |
| 話者ごとのテロップ | ❌ **2経路とも使えないと確定**（③・08-02に実測） |
| カット（無音除去） | ✅ 実装済み。15区間・11.5秒除去 |
| テロップのアニメーション | ✅ pop（0.6→1.06→1.0）/ fade。**行き過ぎ込みで4辺検算NG 0件** |
| 画像インサート（B-roll） | ✅ `--broll`。画面と重なるなら自動でずらす |
| 論点見出しの要約 | ✅ `--headlines` で外から差し替え |
| サムネ5案 | ✅ `pipeline/thumbs.py`。読みやすさを明度差で検査 |
| タイトル案・説明文・チャプター | ✅ 生成済み（`youtube_marketing_pack.md`） |
| **Shorts** | ✅ `pipeline/shorts.py`。31.9秒・仕様の構成どおり |
| イントロ / アウトロ | ❌ 未実装（⑥） |
| ロゴバッジ（nameplate） | ❌ 画像が無く毎回警告（⑥） |

### 「不可」と「適用対象外」は別

`educational_explainer` と `entertainment_variety` は**発話字幕のroleを持ちません**。
前者は6roleすべてがテロップ装置、後者は自分の `caption_font` に
「このスタイルには全発話をカバーする標準字幕ロールが存在しない」と書いており、
role `caption` は `layer_kind: video`（リアクションPiP窓）です。

**直せる欠陥ではなく設計事実**なので、再現率の分母から外してあります
（`style_apply.NoCaptionRoleError`）。分母に入れると、直しようのないものを
永久に未達として数え続けることになります。

---

## ⓪ 着手前に必ず: `verify/preflight.py` を通す

```bash
uv run --with pillow python3 youtube-edit-style-spec/verify/preflight.py <素材>
```

**これを飛ばすと、すでに文書化されている落とし穴を踏みます。**
2026-08-01 の実行では `environment-notes.md` に**書いてあった制約を3件とも踏み**、
40分を無駄にしました。散文の知見は読まれないので、機械で止めます。

---

## ① 2026-08-01 に見つかった「検査の穴」——最重要の教訓

**この日の修正のほとんどは、コードでも数値でもなく検査の穴でした。**
新しい検査を書くときは、以下と同じ形になっていないか必ず確かめてください。

### 1-1. 検査の対象外は「合格」として集計される

`check_four_edges` は `size` を宣言した role しか見ておらず、`max_width_px` しか
持たない role を `continue` で飛ばしていました。結果、**11周ぶん「4辺検算NG 0件」と
報告され続けました**。実際には5件が規定を割っており、うち1件は**画面外（-7.1px）**。

同じファイルの `_rect()` は「size が無ければ推定してでも見る」を実装済みで、
すきま検算と矩形交差検算はそれを使っていました。**後から足した検算だけが
古い前提のまま取り残されていた。**

> **飛ばした対象は「不明」であって「合格」ではない。**
> `continue` した件数を数えない検査は、分母を隠したまま合格率を報告している。

### 1-2. 検査と検査の「あいだ」に落ちる

すきま検算は `0 <= gap` で**負のすきま（＝交差）を除外**していました（交差検査の担当だから）。
その交差検査は**面積比5%未満を捨てて**いました。
→ **面積比5%未満の交差は、どちらの検査にも映らない。** 実在した3件が全部ここに落ちていました。

### 1-3. 同じ概念を2通りに実装しない

影を足す処理がすきま検算の**関数の内側**に閉じ込められており、交差検査は素の矩形で
見ていました。影込みなら5%を超える交差を4%として捨てていた（`japan_vlog` 4%→12%）。

### 1-4. 既定値は必ず組で置く

`font_px` 未宣言に既定 **92** を当てながら、`stroke_px` 未宣言には **0** を当てていました。
`caption_font` は「フォント92／黒フチ12px」を**1組で**規定しているのに片方だけ既定を持たせた結果、
箱が実物より痩せ、境界すれすれの role が余裕ありに見えていました。
この非対称を直しただけで `ai_news_weekly/caption` の下余裕は **+4.9px → −7.1px** に変わりました。

> **同じ違反が、既定値の置き方だけで「軽微」にも「画面外」にも見える。**

### 1-5. 失敗しようがない指標を作らない

APIテキストとローカル時刻の対応付けで「一致被覆 100.0%」と出していましたが、
**埋め合わせ処理の後に数えていた**ので構造的に100%以外になりませんでした。
実測は 60% で、末尾45秒の字幕が3.5秒に潰れていました。

### 1-6. 安全弁は、実際のデータ構造を見てから書く

話者分離の確信度が低いとき色分けしないゲートを
`min(t.get("confidence", 1.0) for t in turns)` と書きました。
`confidence` は**各ターンではなく親側の meta にある**ため既定値1.0が返り、
**ゲートは一度も作動しませんでした**。診断側は `confidence=0.1235 < 0.15` と
正しく警告していたのに、読む側が階層を1つ間違えたせいで、
信用できないラベルで色を塗った動画ができました。

---

## ② 縦（1080x1920）が9本で成立しない ——構造的

`style_apply.py` は方針(a)「座標変換」を採っています。
`centerX/centerY` は正規化済みなのでそのまま、px系は `s = canvas_w/1920 = 0.5625` で一律スケール。

このとき**影込みの余裕は比例しません**。影（左上3px / 右下12px）と要求16pxは定数なので:

| 辺 | 縦で成立する条件 | 横に必要な生余裕 |
|---|---|--:|
| 左・上 | `0.5625 × raw − 3 ≥ 16` | **raw ≥ 33.8px** |
| 右・下 | `0.5625 × raw − 12 ≥ 16` | **raw ≥ 49.8px** |
| role間すきま | `0.5625 × raw − 15 ≥ 16` | **raw ≥ 55.1px** |

落ちた19件の横の生余裕は 19.5〜44.1px で、**全て閾値未満**。
そして**19件すべてが横ではOK**です（例外なし）。

**異方スケール（W比とH比を別々）にしても解決しません**。横方向は `s_w = 0.5625` のまま
（幅を律速に取る限り変わらない）で、落ちた19件のうち**16件は左右の辺**です。

**回避の道は1つだけ**: 縦では px系を `s` ではなく、辺余裕が16pxを満たす最大倍率まで
落とす（内側へ寄せる座標補正）。ただしこれは `position` の正規化値を書き換える行為で、
方針(b)(c)を却下した理由と同じ問題に着地します。**判断が要ります。**

Shorts が第一級ユースケースである以上、避けて通れません。

---

## ③ 話者ごとのテロップ ——**2経路とも使えないと確定**（08-02 実測）

キーを入れて両経路を実測した。**どちらも採用できない。**

### API `gpt-4o-transcribe-diarize`

**日本語音声を英語混じりで書き起こす。** 全164セグメント中、英字のみ52%・日本語37%。
時刻と内容も一致しない（24.12秒で日本語ASRは「これデザインできるんですか?」、
APIは "te iu sotoe narimasu."）。

**言語を指定する手段が無い。**

| 書式 | 結果 |
|---|---|
| `language=ja` | エラーは出ないが効かない |
| `languages[]=ja` | 「このモデルでは非対応」と拒否 |
| `prompt` | 「話者分離モデルでは非対応」と拒否 |

参照音声（`known_speaker_references`）は**書式が特殊**で、4回試して判明した：
**mp3 を base64 にして `data:audio/mpeg;base64,` を付ける**。wav も接頭辞無しも拒否される。
これで interviewer/guest を部分的に識別したが、話者は7人に増え、
疑問文との一致率は質問側33%・回答側10%（**2人問題としてランダム以下**）。

同じ症状（ドイツ語→意味不明な英語）が外部でも報告されている。

### ローカル（MFCC＋ward法）

話者2人・ターン19件・中央値2.4秒と**構造は妥当**だが、
シルエット係数 0.1235（閾値0.15未満）でクラスタが分離していない。

### いま使っているもの

**話法ベース**（疑問文かどうかで質問/回答を分ける）。この素材では確実に効いており、
論点見出し6件が正しく取れる。話者IDより実用的。

**ゲートの教訓**: 確信度の判定が `if route.startswith("local"):` の中にあり、
API経路は素通りしていた。`confidence` が `None` なのを「低くない」と解釈して
壊れたラベルを信用し、論点見出しが6件→1件に崩れた。
**「不明」は「高い」ではない。** 経路によらない妥当性検査（話者数≤4・ターン長≥0.8秒）を
追加済み。さらに**信用できないときはラベル自体を付けない**（表示を止めるだけでは、
`mark_kinds` が話者ベースに切り替わって別の壊れ方をする）。

## ④ 音声の強調は ASR にとって諸刃

`a_enh.wav`（highpass→afftdn→loudnorm→acompressor）は**小さい発話の後半を壊します**。
実測（2分14秒の素材）:

```
asr_enh: 109-134秒 → 「セセセセセ…」107語（whisperの反復幻覚）
asr_raw: 109-134秒 → 正しく聞き取れている
```

→ `reconcile()` の基準を `enh` に固定していたため、**生側が正しく聞き取った45秒がまるごと消えていました**。
現在は「反復幻覚を除いたあとの発話時間が長い方」を基準に選び、空白は他方から補います。
→ **APIにも生の `a16k.wav` を渡します**（強調版を渡すとAPIも末尾を落とす）。

反復幻覚の除去（`drop_repetition`）は照合の**前**に掛けてください。後だと文字対応が総崩れになります。

---

## ⑤ 字幕の分割と折返しは**別の実装**

`captions-quality-v2.md` は「改行は文節（自立語＋付属語）単位」と定めています。
これを `chunk()`（字幕の切り出し）にだけ入れて満足していましたが、
**1つの字幕を2行に折り返す `wrap_text()` は文字単位のまま**でした。

結果、実素材で「新人研修とか絶対いいで／すね。」と語の途中で改行されました。
`style_apply.wrap_text` も文節単位＋禁則処理に直してあります。

> **切り出しと折返しは別の関数。片方だけ直しても直らない。**

---

## ⑥ 残っている作業（2026-08-02 時点）

1. **イントロ / アウトロ** ——一次指示書は「長尺にはイントロとアウトロを生成する。
   アウトロは3-8秒、要点1行、CTA1つ、次動画サムネ枠、固定リンク」と定めている。未実装。
   **文言を決めれば作れる**（素材から導けないので発明はしない）。
2. **ロゴバッジ（nameplate）** ——スタイルが常駐要素として宣言しているが画像が無く、
   毎回「描いていない」と警告が出る。**ロゴ画像を渡せば入る**（`--logo`）。
3. **BGM・効果音** ——音源の供給元が未定。有料契約かフリー音源かで作り方が変わる。
4. **縦（1080x1920）が9本で成立しない** ——構造的（②）。
5. **プラットフォーム連携**（編集→登録→レビュー→公開）——一次指示書が言及しておらず
   仕様が存在しない。**07-10から最大のギャップのまま**。
6. **Shorts の区間選定を自動化するか** ——現状は人（またはLLM）が区間を決めて
   `shorts.py` に渡す。「盛り上がり推定」を規則で書くと素材ごとに外すので、
   **意図的に自動化していない**。自動化するなら根拠を決めてから。

---

## ⑦ 08-02 に踏んだ罠（同じ道を通らないために）

この日は**検査そのものが嘘をつく**型のバグを繰り返し踏んだ。共通するのは
「動いているつもりで一度も動いていない」。

1. **宣言はあるが実装が無い** ——スタイルは `entrance`/`exit` を108箇所で宣言して
   いたのに、パイプラインが読んでおらず全部ハードカットで出ていた。
   論点見出しも同型（役は宣言済み・描画が無い）。
   **スタイルに書いてある装置は、実装されているか必ず突き合わせる。**

2. **検査から外した対象が穴になる** ——lint が「`hard_cut` と `pop` は定義上0秒なので
   対象外」としていたが誤り。`hard_cut` は本当に0秒だが、**`pop` はアニメーションなので
   0秒ではない**。検査対象に戻したら60件の欠落が出た。

3. **偽の安全信号** ——4辺検算は帯やロゴを持つスタイルでは**検査を丸ごと飛ばす**のに
   「NG 0件」と報告していた。`qc-gates.md` が名指ししている型。
   **検査不能は合格ではない。** 現在は `検査不能 N枚` と区別して出す。

4. **同一PNG内部の重なりを誰も見ていなかった** ——全要素を1枚のPNGに描く実装なので、
   レイヤー間QCでは原理的に検出できない（`qc-gates.md` 盲点①）。
   実描画インクの矩形どうしで測る検査を追加。長尺・Shorts とも0件。

5. **例外の握り潰し** ——`except Exception: return 0.0` と書いたため、
   `NameError` で検査が一度も動いていないのに「全部問題なし」に見えた。
   **握り潰さず必ず言う。**

6. **座標系の混在** ——`capseq` に渡る時刻は**カット後**なのに、それを**元素材**に
   当てて画面位置を測っていた（最大11秒ずれ＝別の場面）。`unmap()` を追加。
   往復の誤差0.000秒を確認済み。

7. **測る対象を間違える** ——画像インサートの重なりを「画面が画角のどれだけを占めるか」で
   判定していた。占有率10%でもその真上に置けば覆う。**面積ではなく重なりを測る。**
   数値は「問題なし」だったが、完成品を目で見て気づいた。
   **測定は目視の代わりにならず、目視は測定の代わりにならない。**

8. **numpy 由来の値をそのまま使う** ——真偽値で `json.dump` が落ち、
   float で PIL の `paste` が落ちた（同じ型のミスを2回）。
   **numpy を触る関数の出口で型を揃える。**

---

## 速度（解決済み・踏襲すること）

| | |
|---|---|
| ASR | `openai-whisper`(CPU/FP32) 18分 → **`mlx-whisper`(Metal) 154秒＝7.0倍** |
| エンコード | `libx264` → **`h264_videotoolbox`（HW）** |
| 字幕合成 | overlay 38段 → **連番PNG 1本＋overlay 1段**（19分37秒 → 約4分） |
| 全体 | 2分14秒の素材で **424秒**。同じことを毎回その場で組み立てると約50分 |

`environment-notes.md` L14 は「実描画bboxにクロップすれば29fps」とさらに良い方法を
書いています（今回は全画面PNGのまま。次はこちらを使ってください）。
