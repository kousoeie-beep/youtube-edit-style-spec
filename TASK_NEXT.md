# 次にやること（引き継ぎ）

**最終ゴール**: 「これ風に作って」と言うだけで、**誰でも／自動で**動画編集ができる状態。

2026-08-01 時点。前提知識なしで着手できるように書いています。

---

## いまどこにいるか

| | 状態 |
|---|---|
| 編集の一次指示書 | ✅ 完備（`auto-youtube-edit-agent/`・678行） |
| スタイル定義15本 | ⚠️ **yamlだけで再現できるのは7本**（残り8本が作業対象） |
| 静的検査 | ✅ 26項目・違反0 |
| **実素材で1本通す** | ⚠️ **2026-08-01に初めて実施。完走したが欠陥3件が残っている** |

**11周のスタイル検証は「テロップの幾何」だけを見ていました。**
文字起こし・音との同期・処理速度・実素材は、**一度も検証していません**。
2026-08-01 に初めて実素材（iPhone縦撮り 2分14秒）を通し、以下が判明しました。

---

## ⓪ 着手前に必ず: `verify/preflight.py` を通す

```bash
uv run --with pillow python3 youtube-edit-style-spec/verify/preflight.py <素材>
```

**これを飛ばすと、すでに文書化されている落とし穴を踏みます。**
実際、2026-08-01 の実行では `environment-notes.md` に**書いてあった制約を3件とも踏み**、
40分を無駄にしました。散文の知見は読まれないので、機械で止めます。

検出するもの: ffmpegの機能欠如 / **回転メタデータと実キャンバス** / ラウドネス /
HWエンコードの可否。

---

## ① 実素材で残った欠陥3件【最優先】

作業ディレクトリ: `~/Documents/編集作業/IMG_2514/`（成果物・中間物すべて残置）

### 1-1. ラウドネスが目標に届かない
```
素材 -29.3 LUFS → 出力 -16.3 LUFS（目標 -14 に 2.3LU 不足）
```
`loudnorm` の**1パス測定**では、極端に小さい素材を補正しきれません。
**2パス loudnorm**（1回目で `measured_I` 等を取得し、2回目に渡す）が必要です。

### 1-2. ASRの誤認識が字幕に残る
```
「いつでも見れるですねいいですね」 ← 「見れるんですね」「いいですね」
```
多数決照合（3パス）で一致率 **81.0%**。記録では**4パス（whisper-1 API併用）で 91.25%**。
**APIパスを足せば +6.75pt**。`captions-quality-v2.md` の「ASRの振り分け」を実装してください。

### 1-3. 字幕が無い区間がある
110秒地点に字幕がありません。無音か、省略語（38語）がまとまって落ちた可能性。
`work/uncertain_terms.json` と突き合わせて原因を特定してください。

---

## ② 縦素材にスタイル定義が使えない【構造的】

```
ffprobe が返す寸法 : 1920x1080
実キャンバス       : 1080x1920（rotation=-90 が適用される）
```

**`styles/*.yaml` 15本はすべて 1920x1080 前提**です。`centerX/centerY` も
4辺検算もすきま検算も `size` も、横画面の数値です。
**縦素材に対しては、11周かけた検算が1つも成立しません。**

実測で分かった縦の目安: `font_px 72`（横は92）/ 折返し幅 `W-48×2` / `max_lines: 2`。

**方針の選択が要ります**（未決定）:
- (a) `aspect` ごとに `variant` を切る
- (b) 縦専用のスタイル定義を別に作る
- (c) 横で作って縦にリフレーム（`reframe`）

Shorts が第一級ユースケースである以上、**ここは避けて通れません**。

---

## ③ 字幕の分割は「語境界」では足りない

`captions-quality-v2.md` §4 は「改行は語境界のみ」と定めていますが、
**守っても字幕が割れました**（「教えて|ほしい」「ワザ|マシン」）。

正しくは**文節（自立語＋付属語）単位**。実装は同ファイルの
「『語境界で切る』では足りない」節にあります。実測で 平均11.8字・語の分断ゼロ。

**これは検証クリップでは絶対に出ませんでした**（文言が `"ぱぴぷぺぽバビブベボ"` のような
人工物で、形態素解析を一度も通していなかった）。

---

## ④ 未検証7本の再検証 / ⑤ 「箱だけ」10 role

R11 までの修正を入れた状態は**まだ検証していません**。
11周のうち**5回、直した直後に別の破綻を作りました**。手順は `verify/PROTOCOL.md`。

---

## 速度について（解決済み・踏襲すること）

| | |
|---|---|
| ASR | `openai-whisper`(CPU/FP32) 18分 → **`mlx-whisper`(Metal) 154秒＝7.0倍** |
| エンコード | `libx264` → **`h264_videotoolbox`（HW）** |
| 字幕合成 | overlay 38段 → **連番PNG 1本＋overlay 1段** |

**`environment-notes.md` L14 は「実描画bboxにクロップすれば29fps」とさらに良い方法を
書いています**（今回は全画面PNGのまま。次はこちらを使ってください）。

---

## ゴールに向けて足りないもの

「これ風に作って」で誰でも動く状態にするには、この3つが要ります。

1. **スタイル定義の再現率を 7/15 → 15/15 に**（このリポジトリの本題）
2. **縦対応**（②。Shorts が第一級ユースケースなので必須）
3. **プラットフォーム連携**（編集 → 登録 → 人間レビュー → 承認 → 公開）。
   一次指示書はここに一切言及しておらず、**07-10から最大のギャップのまま**

1 と 2 はこのリポジトリで完結します。3 は外部の実装が要ります。
