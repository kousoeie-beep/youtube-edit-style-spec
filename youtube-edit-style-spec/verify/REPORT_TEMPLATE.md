# R<N>（<N>周目）再実装検証 — <style_a> / <style_b>

**進行中（追記式）。** 途中で落ちても直前までの実測が残るよう、節ごとに書き足す。

対象yaml（**1文字も書き換えない。git操作も一切しない**）:
- `<path_a>`（mtime / bytes）
- `<path_b>`（mtime / bytes）

着手時刻: <JST>

**方法**: `verify/PROTOCOL.md` に従う。
数値はすべて実測で、出所（どのPNG・どの座標計算）を各節に併記する。

---

## 0. 対照窓の時刻（規約⑬）

| クリップ | 本編 | 対照窓 | 何を破ったか |
|---|---|---|---|
| <name>_main | [0, X) | — | — |
| <name>_ctl | — | [X, Y) | <規約名> |

**この時刻の FAIL は実害ではない。**

## 1. <style_a>

### 判定: **可 / 不可**

### QC結果
```
=== <name>_main === elements=N overlaps=M status=... (exit N)
```
`element_count` は、生成レイヤー数から動画その他の明示的な検査除外レイヤーを差し引いた「検査可能レイヤー数」Nと一致（＝QCを信用してよい）/ 一致しない（＝信用しない）

### 1-1. <変更名> — **効いた / 効いていない / 効いたが別の破綻を作った**

| 項目 | yamlの宣言 | 実測 | 判定 |
|---|---|---|---|

### 1-2. 新規欠陥

**実測**:
**目視**（frames/...）:
**修正案**: フィールド名・数値・導出の3点セット

## 2. <style_b>

（同上）

## 3. 自分の側の訂正

自分の実装ミスと yaml の欠陥を混同しないための記録。

| # | 内容 |
|---|---|

## 4. まとめ

| 対象 | 判定 | 一行 |
|---|---|---|

**もっとも重要な一行**:
