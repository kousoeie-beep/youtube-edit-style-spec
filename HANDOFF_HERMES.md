# Hermes への引き継ぎ

2026-08-04 作成。**このファイルだけ読めば着手できる**ように書いています。

---

## 0. 30秒で状況を掴む

「これ風に作って」と言うだけで、誰でも／自動で YouTube 用の動画編集ができる状態を作る、
という長期ゴールがあります。**編集そのものはほぼ動きます。** 素材1本を渡せば、
カット・字幕・テロップ・アニメーション・画像インサート・音声整音・サムネ5案・
Shorts・公開パックまで1コマンドで出ます。

**残っているのは主に「編集の外側」です。** 特にプラットフォーム連携
（編集 → 登録 → 人間レビュー → 承認 → 公開）は一次指示書が一切言及しておらず、
2026-07-10 から最大のギャップのままです。ここが Hermes の本命だと考えています。

---

## 1. このリポジトリは PUBLIC です

```
https://github.com/kousoeie-beep/youtube-edit-style-spec
```

**顧客素材・実映像・文字起こし・営業資料は入っていません。入れないでください。**
検証に使った素材には顧客の営業資料（価格・話法）が写り込んでおり、
公開リポジトリに置けません。素材はローカルにのみ存在します。

このリポジトリに入れてよいのは、**仕様・コード・検証手順・教訓**だけです。

---

## 2. 最初にやること

```bash
git clone https://github.com/kousoeie-beep/youtube-edit-style-spec.git
cd youtube-edit-style-spec
python3 youtube-edit-style-spec/verify/preflight.py     # 環境・回転・音量を機械で検査
```

`preflight.py` は**着手前に必ず通してください**。環境差（フォント・ffmpeg・
ハードウェアエンコーダの有無）を機械で潰すためのものです。

素材を1本渡して通す：

```bash
uv run --with pillow --with fugashi --with unidic-lite --with mlx-whisper \
  --with pyyaml --with numpy --with scipy --with librosa --with soundfile \
  python3 pipeline/run.py <素材.mov> --out <出力先> --style kirinuki
```

実測 10〜11分（2分14秒の縦素材・Apple Silicon）。

---

## 3. 何が動いていて、何が動いていないか

| 項目 | 状態 | 根拠 |
|---|---|---|
| カット（無音除去） | ✅ | 15区間・11.5秒除去 |
| 字幕 | ✅ | 56件・4辺検算NG 0件 |
| 論点見出し | ✅ | 6件。`--headlines` で文言差し替え可 |
| テロップのアニメーション | ✅ | pop（0.6→1.06→1.0）/ fade。行き過ぎ込みで4辺NG 0件 |
| 画像インサート | ✅ | `--broll`。画面と重なるなら自動でずらす |
| 音声 | ✅ | −15.0 LUFS / TruePeak −2.2 dBFS |
| サムネ5案 | ✅ | `pipeline/thumbs.py`。読みやすさを明度差で検査 |
| タイトル案・説明文・チャプター | ✅ | 生成済み |
| Shorts | ✅ | `pipeline/shorts.py`。31.9秒・仕様の構成どおり |
| **話者分離** | ❌ | **2経路とも実測で使えないと確定**（§5） |
| **イントロ / アウトロ** | ❌ | 未実装。**文言を決めれば作れる** |
| **ロゴバッジ** | ❌ | 画像が無く毎回警告。`--logo` で渡せば入る |
| **BGM・効果音** | ❌ | 音源の供給元が未定 |
| **縦（1080x1920）で成立するスタイル** | ⚠️ 4/13 | 残りは構造的（`TASK_NEXT.md` ②） |
| **プラットフォーム連携** | ❌ | **仕様が存在しない。ここが本命** |

---

## 4. 「もっと完璧にする」作業一覧（Hermes の実作業）

優先順に並べています。**各項目に受け入れ条件（測れる形）を付けました。**
「良くなった気がする」で終わらせないためです。

### 回帰の基準値（着手前にこれを記録すること）

いま出ている数字です。**改善作業でここが悪化したら、それは改善ではありません。**

| 指標 | 現在値 | 測り方 |
|---|---|---|
| 完成尺 | 123.2秒（素材134秒） | `ffprobe` |
| ラウドネス | −15.0 LUFS / TP −2.2 dBFS | `loudnorm` 1パス目 |
| 黒フレーム | 0 | `blackdetect` |
| 4辺検算NG | 0件 | `run.py` のログ |
| 要素どうしの重なり | 0件 | 同上 |
| 字幕 | 56件 | 同上 |
| 論点見出し | 6件 | 同上 |
| 総所要 | 627秒 | 同上 |
| Shorts | 31.9秒 / −14.9 LUFS | 同上 |

---

### P1. レンダリングを10倍速くする（**文書化済みなのに未適用**）

`youtube-edit-style-spec/environment-notes.md` L14 にこう書いてあります。

> フルキャンバス透過PNGの多段overlayは激遅（~1fps）→ PNGを実描画領域だけに
> bboxクロップしてoverlay座標指定すれば~29fpsに改善（showcase実証）

**実証済みなのに、現在の実装は全画面PNGのままです。** 総所要627秒のうち
レンダリング段が373秒（60%）を占めます。1064枚の全画面透過PNGを overlay しています。

**受け入れ条件**
- 同一素材で、上の基準値（尺・ラウドネス・4辺・重なり・字幕件数）が**すべて不変**
- 総所要が**半分以下**
- 出力フレームの画素差分が実質ゼロ（PSNR 45dB 以上など、方法は任せます）

**罠**: PNG をクロップすると overlay 座標を自分で持つ必要があります。
アニメーション（pop は1.06倍に行き過ぎる）でインクが動くので、
**クロップ範囲を固定すると切れます**。フレームごとに bbox を持つこと。

---

### P2. 噛み・言い直しのカット（**一次指示書の要求だが未実装**）

一次指示書 L24：

> 無音、噛み、言い直し、長すぎる間、不要シーンを自動カットした完成動画

`find_cuts()` は**無音（語間ギャップ）しか見ていません**。噛み・言い直し・
不要シーンは実装がありません。「長すぎる間」は無音判定に含まれます。

**受け入れ条件**
- 検出結果を**カットする前に一覧で出す**（時刻・該当テキスト・種別・確信度）
- 実素材で人が確認し、**誤検出率を数える**。数字を `TASK_NEXT.md` に残す
- 自動削除は誤検出率が十分低いと確認できてから。**それまでは提案止まり**

**罠**: カットは不可逆です。そして「言い直し」は日本語では
**強調や言い換えと区別がつきにくい**（「これは…これを…」が言い直しとは限らない）。
自信のない検出を消すと、話が飛んで意味が壊れます。**消さない方の誤りを選ぶこと。**

---

### P3. プラットフォーム連携（**仕様が存在しない。設計から**）

編集 → 登録 → 人間レビュー → 承認 → 公開。一次指示書が一切言及しておらず、
2026-07-10 から最大のギャップのままです。**まず仕様を書いてください。**

**受け入れ条件**
- 仕様書がある（どの状態で何が起き、誰が承認し、失敗したらどう戻すか）
- **承認は人間が握る**設計であること。自動公開しないこと
- 公開前に守秘度の確認が入ること（下の「罠」）

**罠**: 検証素材には顧客の営業資料（価格・話法）が**判読できる状態で写っています**。
サムネ5案のうち4案がそうです。**公開経路に守秘度の関門が無いと事故になります。**

---

### P4. 縦（1080x1920）で成立しないスタイル9本

`TASK_NEXT.md` ② に分析があります。構造的な作り直しです。

**受け入れ条件**: 縦で成立するスタイルが 4/13 から増えること。
成立しない場合は「不可」ではなく**理由を構造として書く**こと
（`educational_explainer` のように「発話字幕のroleを持たない」が設計事実なら、
再現率の分母から外す。直せない欠陥を永久に未達として数えない）。

---

### P5. 素材か判断があれば閉じられるもの（設計課題ではない）

| 項目 | 必要なもの |
|---|---|
| アウトロ（3-8秒・要点1行・CTA1つ） | **文言**。素材から導けないので発明しない |
| ロゴバッジ（nameplate） | **ロゴ画像**。`--logo` で渡せば入る |
| BGM・効果音 | **方針**（有料契約かフリー音源か） |

これらは Hermes が判断するものではありません。こうすけさんに聞いてください。

---

### P6. QC の残る盲点

`youtube-edit-style-spec/qc-gates.md` に5つの盲点が列挙されています。
08-02 に **①（同一PNG内部の重なり）** と **偽の安全信号** は塞ぎました。
残りは未確認です。特に：

- **盲点④ Pillow の低アルファ描画**: テクスチャを低アルファで敷くと
  「全画面カードのはずが穴だらけ」になる。**アルファは255に統一**する
- **盲点⑤ グロー/ぼかしはQCをすり抜ける**: alpha<10 がトリムされ、
  実際は重なっているのにQCが検出しない。**必ずフレーム目視**

**受け入れ条件**: 各盲点について「該当する実装があるか」を調べ、
無ければ「該当なし」と根拠つきで記録する。**調べていないものを「問題なし」と書かない。**

---

## 5. 話者分離は「できない」と確定しています（再挑戦の前に読んでください）

キーを入れて両経路を実測しました。**どちらも採用できません。**

**API `gpt-4o-transcribe-diarize`**: 日本語音声を英語混じりで書き起こします
（実測: 全164セグメント中、英字のみ52%・日本語37%）。時刻と内容も一致しません。
そして**言語を指定する手段がありません**。

| 書式 | 結果 |
|---|---|
| `language=ja` | エラーは出ないが効かない |
| `languages[]=ja` | 「このモデルでは非対応」と拒否 |
| `prompt` | 「話者分離モデルでは非対応」と拒否 |

参照音声（`known_speaker_references`）は**書式が特殊**です。4回試して判明しました：
**mp3 を base64 にして `data:audio/mpeg;base64,` を付ける**。wav も接頭辞なしも拒否されます。
これで話者を部分的に識別しましたが、話者は7人に増え、疑問文との一致率は
質問側33%・回答側10%でした（**2人問題としてランダム以下**）。

**ローカル（MFCC＋ward法）**: 話者2人・ターン19件・中央値2.4秒と構造は妥当ですが、
シルエット係数 0.1235（閾値0.15未満）でクラスタが分離していません。

**いま使っているのは話法ベース**（疑問文かどうかで質問/回答を分ける）です。
この素材では確実に効いており、話者IDより実用的でした。

再挑戦するなら、**専用ライブラリ（pyannote 等）は未検証**です。そこが唯一の未踏路です。

---

## 6. 意図的に自動化していないもの

「賢くやろうとして外す」より「人が決めて渡す」方が確実だと判断した箇所です。
**自動化する場合は、根拠を決めてから**にしてください。

| 箇所 | なぜ自動化しないか |
|---|---|
| 論点見出しの文言 | 規則で縮めると日本語が不自然になる。切れ目は素材から取り、**文言だけ外から渡す** |
| Shorts の区間選定 | 「盛り上がり推定」は素材ごとに外す。区間は人（またはLLM）が決める |
| サムネの背景フレーム・文字 | 同上。ただし**読みやすさは機械で検査**して基準未満なら落とす |

---

## 7. 踏んだ罠（同じ道を通らないために）

`TASK_NEXT.md` ⑦ に8項目あります。**すべて「検査が動いているつもりで一度も
動いていない」型**でした。特に効くのは次の3つです。

**検査不能は合格ではない。** 4辺検算は帯やロゴを持つスタイルでは検査を丸ごと
飛ばすのに「NG 0件」と報告していました。現在は `検査不能 N枚` と区別します。

**「不明」は「高い」ではない。** 話者分離の確信度ゲートが API 経路を素通りさせ、
`confidence=None` を「低くない」と解釈して壊れたラベルを信用し、
論点見出しが6件→1件に崩れました。

**測る対象を間違えると数字は嘘をつく。** 画像インサートの重なりを「画面の占有率」で
判定していました。占有率10%でもその真上に置けば覆います。
数値は「問題なし」でしたが、完成品を目で見て気づきました。
**測定は目視の代わりにならず、目視は測定の代わりにもなりません。**

---

## 8. 運用上の注意（重要）

**パイプライン本体はリポジトリ外で編集されていました。** `~/Documents/編集作業/pipeline/`
で編集し、リポジトリへは手でコピーする構成だったため、**コピーを忘れると git は
「変更なし」と言い、リポジトリが古いまま「最新」に見えました**（実際に3コミットぶん
古くなりました）。今日ずっと戦っていた「偽の安全信号」と同じ形です。

**2026-08-04 に解消済み。** `~/Documents/編集作業/pipeline` は
`handoff_hermes_20260731/pipeline` へのシンボリックリンクになっており、
編集がそのままリポジトリに入ります。**同期を人手に頼る構成へ戻さないでください。**

もう1点。作業ディレクトリが `~/Documents`（iCloud 同期対象）にあり、
ディスクが逼迫すると**ファイルの実体がクラウドへ退避されて読めなくなります**
（`ls` は通るのに全ファイルが I/O エラーになる）。**08-02 に2度これで作業が止まりました**
（2度目は git の `index.lock` が書けずコミットが失敗した）。
**同期対象外の場所で作業することを勧めます。**

---

## 9. 関連文書

| ファイル | 中身 |
|---|---|
| `TASK_NEXT.md` | 引き継ぎの正本。現状・残作業・踏んだ罠の詳細 |
| `auto-youtube-edit-agent/auto_youtube_edit_agent.md` | **編集の一次指示書**（678行）。カット基準・字幕規則・テロップenum・音声処理・サムネ5案・品質チェック・edit_log義務 |
| `youtube-edit-style-spec/SKILL.md` | 実行スキル（一次指示書に環境知見・字幕品質v2・納品ルールを上書き） |
| `youtube-edit-style-spec/qc-gates.md` | **QCの盲点集**。「1つも検査していないのに合格に見える」型の記録 |
| `youtube-edit-style-spec/styles/*.yaml` | スタイル定義15本＋`lint.py`（29項目の静的検査） |
| `pipeline/README.md` | 工程・実測値・既知の限界 |

**着手前に `qc-gates.md` を読んでください。** そこに書いてある罠を、
書いた本人が後から踏みました。読めば防げます。
